import styled from 'styled-components';

export const SColumnContainer = styled.div`
  padding-left: 10px;
  background: var(--bg-100);
  width: 100%;
  display: flex;
  flex-direction: row;
  overflow-x: scroll;
  height: 100%;
`;
