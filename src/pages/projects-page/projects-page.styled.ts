import styled from 'styled-components';

export const SProjectPage = styled.div`
  background: var(--bg-100);
  min-height: 100vh;
  padding: 0px;
  width: 100vw;
`;

export const SProjectMainSection = styled.main`
  padding: 20px;
  margin-left: var(--sideBarWidth);
  width: calc(100vw - var(--sideBarWidth));
`;
