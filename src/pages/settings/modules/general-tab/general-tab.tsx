import React from 'react';
import { SectionTitle } from '../../components/section-title/section-title';

export const GeneralTab = () => {
  return (
    <div>
      <SectionTitle title={'Общие настройки'} desc={'Настройте проект FF'} />
    </div>
  );
};
